<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 50 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "我的作品", current_user %>
</span>
<span>
  <%= pluralize(current_user.books.count, "作品") %>
</span>